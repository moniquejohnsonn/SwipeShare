import SwiftUI

struct ReceiverHomeView: View {
    
    var body: some View {
        VStack {
            Text("Receiver Home")
                .font(.largeTitle)
                .padding()
        
        }
    }
}

